services:
  homelab-api:
    build: .
    container_name: homelab-api
    command: >
      uvicorn app.main:app
      --host 0.0.0.0
      --port 8000
      --reload
    ports:
      - "8000:8000"
    volumes:
      - .:/code
    restart: unless-stopped

  homelab-tests: 
    build: . 
    container_name: homelab-tests
    environment: 
      - PYTHONPATH=/code
    volumes: 
      - .:/code 
    command: pytest -vv --cov=app --cov-report=html
    
  ollama:
    image: ollama/ollama:latest
    container_name: ollama
    deploy: 
      resources: 
        reservations:
          devices: 
            - capabilities: [gpu]
    ports:
      - "11434:11434"
    volumes:
      - ollama-data:/root/.ollama
      - .:/workspace
    restart: unless-stopped
volumes: 
  ollama-data: